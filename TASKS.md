# TASKS.md — 税額ポケット

## いまここ（2026-09-07 時点）

**所内配布は完了している。** 第1版スコープの計算7メニュー（退職金・減価償却費・印紙税・登録免許税・
延滞税/利子税・ふるさと納税・相続税の概算）と参照2画面（リンク集・法人税の早見表）、
「和暦・西暦」「更新の確認」が職員の端末で動いている。
公開先は https://sakurataxsys.github.io/tax-pocket/ 。

**★作業ツリーに未コミットの変更がある（2026-09-07 のセッション）。まずここを片づけること。**

- **「報酬の源泉徴収」メニューを新設した**（所法204条。要否の判定と税額）。実装・テスト・文書は完了。
  **テスト393件が全件通り、画面でも動作を確認済み。ただしコミットしていない。**
  内容は判断ログ **D-40** に全部書いてある（根拠条文の一覧・敵対的レビューで捕まった欠陥・残した限界）
- あわせて **`styles/app.css` の選択欄に▼の印を足した**（全画面に効く。理由は D-40 の末尾）
- `src/version.js` と `sw.js` を **v19 → v20** に上げてある。**公開中の版は v19 のまま**

**次のセッションの最初の作業＝この変更をコミットして公開するか、本人の判断を仰ぐこと。**
公開したら、職員への案内に書く版は **v20** になる（アプリの「更新の確認」の表示と合わせる）。

- **別セッションで走らせた退職金の修正も、2026-09-08 にこの作業ツリーへ畳んだ**（判断ログ **D-41**）。
  `src/calc/taishokukin.js` の `fukko_bunshi()` に `Number()` を足し、テスト1本を足したもの。
  **ワークツリー `claude/musing-goodall-aeb362` は用済み**（中身は全部こちらに入っている。撤去は本人の判断）

**★版番号は「ブランチを切ったとき」ではなく「公開するとき」に決める。**
上の2つは別々のツリーで走ったため、**両方が独立に v19 → v20 へ上げた**。
1つの版としてまとめて公開するので v20 のままでよいが、
**同じ版名で中身の違うものを2度配ると、先に受け取った端末には後の版が永久に届かない**
（cache-first のため。D-16・D-25 と同じ病気）。
`tests/version.test.js` は同じツリーの2ファイルしか突き合わせないので、この衝突は検出できない。

**職員への案内はユーザーが行う。2026-08-20 の案内で版 v18 を伝えた。**
次に案内するときは、そのとき公開している版を書く（アプリの「更新の確認」の表示と合わせる）。

**kintone のポータルに導線を置き、職員へ周知した（2026-08-20・すぐ下の専用節）。**
**これで第1ステップの「配り方」は完結した。**職員は今後、チャットの過去ログを遡らずに
ポータルのアイコンからいつでも辿り着ける。

**次にやること＝成功条件の判定**。判定は利用実績ではなく**職員への聞き取り2問**で行う
（①関与先で実際に開いたか ②次にどのメニューが欲しいか）。この2問は kintone のレコードの
説明にも書いてあるので、宛先へ直接返ってくる。**②の回答が、次に作るメニューを決める材料になる。**

Cloudflare へのアップテストは**測定完了・撤収済み**（下の専用節）で、tax-pocket 側にやることは残っていない。

### kintone ポータルへの設置（2026-08-20・**設置・周知とも完了**）

**なぜレコード経由なのか、更新のとき何を触るのかは判断ログ D-38 を読むこと。**
前日に「上手な税務調査の受け方」で作った導線と同じ形（`../TaxAuditManual/配布準備.md` §3-2）。

用意したもの（`assets/`。作り直しは `py assets/ポータルアイコン生成.py`。手で画像を編集しない）

- `ポータルアイコン_税額ポケット.png` … 透明背景版。**絵柄はアプリ本体のアイコン（`icons/icon-512.png`）を丸く切ったもの**（判断ログ D-39）
- `ポータルアイコン_税額ポケット_白背景.png` … **お知らせ掲示板へ貼るのはこちら**（クリップボード経由では透明が保てないため）
- `QRコード_税額ポケット.png` … 中身は公開URL1つだけ。別のデコーダで読み直して一致を確認済み

#### どう置いたか（更新するときはここを見る）

1. kintone の「ファイル管理」アプリ（スペース「業務管理システム」）でレコードを1件作る。レコード名は **「税額ポケット」**
2. 本文に下の「レコード本文」をそのまま貼る
3. 添付に `assets\QRコード_税額ポケット.png` と `docs\配布手順書.html` の2つを入れて保存する
4. 保存したレコードの画面URLを控える（**添付ファイルの直リンクは使わない**。差し替えでURLが変わるため）
5. ポータルのお知らせ掲示板を編集し、丸アイコン列の末尾（「上手な税務調査の受け方」の右隣）に
   `assets\ポータルアイコン_税額ポケット_白背景.png` をクリップボード経由で貼る。
   **区切りは全角スペース4つ・画像は100px幅**（詰めると上の段の列とずれる）
6. 貼った画像に 4. のレコードURLをリンクする
7. 保存し、PCでアイコン→レコードが開くことを確かめる

**1.〜7. は 2026-08-20 に実施済み。** アイコンはお知らせ掲示板の2段目、
「上手な税務調査の受け方」の右隣に入っている。QRの画像はレコードでクリックすると
画面いっぱいに拡大され、その状態でスマートフォンから読み取れる。

**周知も 2026-08-20 に実施済み**（下の「職員への案内文」を流した）。
**kintone は掲示板を更新しても通知が飛ばない**ので、次に何かを変えたときも
所内の連絡で別途流すこと。

**レコード番号とレコードのURLはここに書かない**（このリポジトリは公開される）。
実物はポータルのアイコンから辿る。

#### レコード本文（いま入っているもの）

```
税額ポケット（関与先で税額を出すための計算ツール）

添付のQRコードの画像をクリックして大きく表示し、
スマートフォンのカメラで読み取ってください。
開いたページをホーム画面に追加してください。
追加しないと、電波の届かない関与先で使えないことがあります。

https://sakurataxsys.github.io/tax-pocket/

PCのブラウザ（Edge・Chrome）でも、上のURLから同じように
使えます。事務所で試すだけならPCが早いです。

使い方は添付の「配布手順書.html」にあります。
保存して Edge か Chrome で開いてください。

税制改正を反映したときは、事務所から所内の連絡でお知らせします。
自分の端末に反映されているかは、アプリの「更新の確認」で
確かめられます。
```

#### 職員への案内文（2026-08-20 に流したもの）

**連絡手段の名前は本文に書いていない**（チャットワークを畳む予定のため。判断ログ D-39）。
いま使っている所内の連絡にそのまま流せる。

**宛先の実名もここには書かない**（公開リポジトリのため。判断ログ D-39 の 2-1）。
実名は kintone のレコードの説明に入れてある。担当が交代したら、直すのはそこ1か所。

```
【ご案内】税額ポケットを kintone のポータルに置きました

これまでチャットのお知らせだけでご案内していた「税額ポケット」を、
kintone のポータル（お知らせのアイコン欄）から開けるようにしました。
アイコンをクリックするとファイル管理のレコードが開きます。

★まだ端末に入れていない方へ（1回だけの作業です）
1. ポータルのアイコンから「税額ポケット」のレコードを開く
2. 添付のQRコードをスマートフォンのカメラで読み取る
3. 開いたページをホーム画面に追加する
　 iPhone … 共有ボタン（□から↑）→「ホーム画面に追加」
　 Android … 右上の︙ →「ホーム画面に追加」
4. 追加したアイコンから開き、メニューを上から順に1回ずつ表示する（1分ほど）

4. までやっておくと、電波の届かない関与先でも計算できます。
ブラウザのお気に入りに入れただけでは、この効果はありません。

★PCでも使えます
スマートフォンを使わなくても、PCのブラウザ（Edge・Chrome）で
レコードに書いてあるURLを開けば、同じように使えます。
事務所で中身を見てみるだけなら、こちらが早いです。

くわしい使い方は、レコードに添付した「配布手順書.html」にあります。
保存して Edge か Chrome で開いてください。

いま入っている版は v18 です。アプリの「更新の確認」で見られます。

・計算結果は概算です。申告その他の税務判断の根拠には使えません。
・入力した金額は端末の外に出ません。この端末の中だけで計算しています。

関与先で実際に開いてみた感想と、次に欲しいメニューを、
事務所の担当まで聞かせてください。
```

#### 確認した（2026-08-20）

- **ファイル管理アプリのアクセス権は `Everyone` にレコード閲覧が付いている。**
  レコード単位の制限も設定されていない。**全職員が開ける**（税務調査の受け方のときからの
  持ち越しだった宿題は、これで片づいた）

### 公開した（2026-08-06）＝版 v14

**v10 → v14 を公開した**（`5aeec20..656e1c1`）。本番で次を確認済み。

- メニューに「ふるさと納税」「和暦・西暦」が出る／「更新の確認」が v14 になる
- `data/furusato.json` の日付（2026年8月6日）が「更新の確認」に出る
- 給与収入500万円・社会保険料70万円・独身で限度額 **58,000円**（割合84.895％・所得割240,500円）＝
  条文から手で計算した値と一致

**職員への案内（チャットワーク）はユーザーが行う。**

公開した内容（記録）：

1. **「和暦・西暦」**（旧「元号変換」。判断ログ D-26）
2. **所得税エンジン**（D-27。画面が無い内部モジュール）
3. **所得税エンジンの令和8年分対応**（D-28）
4. **ふるさと納税シミュレーター**（D-29。2026-08-06 に完成）

テストは **303件**が通り、`node tools/verify_besshi5.mjs` も令和7年分・令和8年分の両方で一致する。

### 公開後にやること

- **職員から「メニューにふるさと納税が出ない」と申告が出たら**、「アプリを閉じて開き直す」を
  **2回**試してもらう（**1回では変わらない**。2026-08-20 の実測＝判断ログ D-36）。
  それで直れば版付き cache-first の想定どおり（判断ログ D-16）。直らなければ
  ブラウザのキャッシュ削除 → それでも駄目なら配信側を疑う
- **職員への聞き取り2問**（①関与先で実際に開いたか ②次にどのメニューが欲しいか）＝これが成功条件

### 2026-08-06 に作ったもの：ふるさと納税シミュレーター（判断ログ D-29）

出力は**限度額だけ**。**計算方式は画面上部のセレクトで切り替える**（既定＝正式方式）。
入力はよく使う項目だけ常時表示し、残りは「詳しく入力」の折りたたみに入れた。

**着手前に計画を敵対的レビュー（Fable）にかけ、致命的1件・重要6件・軽微7件が出た。** すべて実装前に
計画へ反映してある。とくに次の2つは、出す数字が変わる指摘だった。

- **致命的**：扶養親族の入力に「16歳未満」の欄が無いと、所得金額調整控除（23歳未満の扶養親族で発動）が
  漏れる。給与収入850万円超＋16歳未満の子という**ごくありふれた属性**で、限度額が約4,000〜5,000円
  **系統的に過大**に出る
- **重要**：地方税法37条の2第11項の**2号と3号を取り違えていた**（90％は2号）。数値は同じでも、
  画面に出す根拠の号・データのキー名・テストの期待根拠が全部ずれるところだった

レビュアーの引用は鵜呑みにせず、37条の2第11項・314条の7第11項・附則5条の5・附則5条の6・附則34条・
所得税法78条・改正法附則3条を**自分でも原文で取り直して**から実装した。

**セキュリティ診断も1回回した**（読み取り専用サブエージェント）。重大0・中1・低3で、
**中1・低3ともその場で対応済み**（残るのは既出の「初回コミットの作者メール」だけ）。
中の指摘は「人数欄の値がそのままループ回数になり、`100000000` と打つと関与先の前でタブが固まる。
しかもその処理が `show_result()` の外なので、前の人の計算結果が画面に残る」というもの。
`docs/判断ログ.md` の「セキュリティ診断の記録」に全文を残した。

### 公開した（2026-08-09）＝版 v15 ＝ 判断ログ D-30

**ユーザーが実際に踏んだ画面の不具合・要望を3点直した。**

1. **西暦を選んでも和暦の欄が消えなかった**（`.field-row{display:grid}` がブラウザ既定の
   `[hidden]{display:none}` を上書きしていた）→ `[hidden]{display:none !important}` を全体に置いた
2. 「変換の方向」をプルダウンから**ラジオ**に変えた（`radio_input()` を `src/ui.js` に新設）
3. **入力欄をタップしたら中身を全選択**するようにした（人数欄の `0` を手で消す必要がなくなる）

**3つとも 2026-08-10 に iPhone の実機で確認した**（v16 の確認時。下の「v16」）。
とくに3（タップで全選択）は iOS で挙動が割れやすい箇所だったが、**人数欄の `0` が
`02` にならずに置き換わることを実機で確認**した。

**聞き取り後の版に束ねる3点**（実使用データ抜きで判断できるもの。D-30）：

- ふるさと納税の結果ラベルに「**目安**」を入れる（この画面だけ性質が入っておらず確定値に見える）
- `--muted` を1段暗くする（薄グレー地の上で4.24:1＝AA基準4.5:1を割っている）
- `.warn` の文字を濃く一段大きく（結果の意味を変える文言が、入力欄の補足と同じ重みで出ている）

### 公開した（2026-08-10）＝版 v16 ＝ 判断ログ D-31・D-32・D-33

**第1版スコープの計算7メニューが揃った。** 中身は4つ。

1. **相続税の概算**（D-31）。出力は総額＋納付総額の2パターン。
   税率表は相続税法16条の表から自動生成する（`node tools/fetch_sozokuzei.mjs`）
2. **セキュリティ診断の指摘への対応**（重大0・中1・低3。中1・低3とも対応済み）
3. **和暦・西暦の年齢を1つの数字にした**（D-32。`今年の誕生日で／満76歳`）
4. **メニューを性質で並べ替え、塊に見出しを付けた**（D-33。
   便利ツール／計算シミュレーター／調べもの・早見表／アプリの状態）

**2026-08-10、iPhone の実機で6点すべて確認できた。** ここで積み残しが2つ埋まった。

- ★**「既に版を持っている端末が上がる経路」を初めて観測した。** v15 を持った端末が、
  閉じて開き直すだけで v16 になった。2026-08-06 に観測できたのは新規訪問の経路だけで、
  この経路はずっと未観測だった（下の「積み残した確認」）
- ★**v15 で入れた「タップで全選択」が iOS で効くことを確認した。**
  人数欄の `0` が `02` にならずに置き換わる
- あわせて、相続税の画面が最後まで到達できること、メニューの見出しと並び、
  和暦・西暦のラジオと年齢表示、**機内モードで相続税が計算できること**を確認した

★**未確認が2件残る。** ①暦年贈与の加算期間の経過措置は国税庁タックスアンサー（二次情報）で
押さえており、改正法附則の原文は未確認（**効いてくるのは令和9年1月から**）②**Android は依然未確認**。
いずれも下の「積み残した確認」。

### 公開した（2026-08-10）＝版 v17 ＝ アイコンの差し替え ＝ 判断ログ D-34

**ホーム画面のアイコンを、事務所のロゴ（桜）とブランド配色を使ったものに差し替えた。**
図案は生成AIに描かせた案を下絵にしており、紺の地に生成りのポケットから金の電卓が覗く。
作り直すときは `tools/icon-source.html` を開いて「書き出す」（手で画像を編集しない）。

- **設計原則5を「禁止」から「何を守りたいか」に書き換えた。** 守るのは全画面の免責1行で、
  名前やロゴの有無ではない。**意匠の是非をこの条文で毎回止めないこと**（D-34）
- ★★**既にホーム画面に追加済みの端末では、アイコンは差し替わらない（2026-08-10 iPhone で確認）。**
  アプリの中身は版付き cache-first で v17 に上がるが、**ホーム画面のアイコンはOSが追加時に
  取り込んだものが残る**。変えるには**一度削除して追加し直す**しかない
- したがって**アイコンの変更は、それ自体を理由に職員へ案内しない**。次に「削除して追加し直す」が
  必要になる場面（機種変更・追加し直し）で自然に新しくなれば足りる。
  「アイコンが変わらない」という申告が出たら、上のとおり説明する
- 職員への案内では**アイコンに触れない**（次回以降で足りる、というユーザー判断）。
  ただし**案内文の版は v17** と書くこと（v16 と書くと「更新の確認」の表示と食い違う）

### 公開した（2026-08-14）＝版 v18 ＝ 判断ログ D-35

**結果カードの折り返しを直した。** 退職金の結果で「一般退職手当等」の「等」だけが次の行に落ちていた。
`.result__subs` の見出し列を `max-content` → `fit-content(50%)` にして、狭いときに折り返すのを
**値ではなく見出しの側**にした（金額は右揃えで桁を追うものなので、割れると読めなくなる）。

- ★**`sw.js` の `SHELL` に載っているのは `src/` だけではない。** `styles/app.css` も入っているので、
  **CSS だけの修正でも版を上げないと、既に入れてある端末には永久に届かない。**
  `sw.js` の注意書きが「ロジック（src/ 配下）を変更したら」と読めたので、SHELL 全体を指す書き方に直した
- あわせて `docs/配布手順書` を v18 の中身に合わせた
- **実機での確認は未実施**（スマホ幅の目視は開発者ツールの 375×812 で行った）

### Cloudflare へのアップテスト（2026-08-20・**測定完了・撤収済み**）

**正本＝ `../keiei-notes/memory/内製ツール配布基盤-Cloudflare適用可否と税額ポケット判定.md`。**
着手前にそちらを読むこと（ここに書くのは tax-pocket 側で効く要点だけ）。

**やること**＝税額ポケットを Cloudflare Workers（Static Assets＝静的ファイルをそのまま配る機能）に
**並走**で載せ、検証環境として使う。**GitHub Pages は止めない。職員には再配布しない**
（つい最近配ったばかりで、再配布すると職員が混乱するため）。

- **Workers を選んだ**（Pages ではない）。設定は `wrangler.jsonc` に `name` / `compatibility_date` /
  `assets.directory` の3つだけ。ビルド工程が無いのでリポジトリ直下を指せば足りる。
  ブランチごとのプレビューURL（本番と別の検証用URL）は Workers でも同じように立つ＝Pages を選ぶ理由が無い
- **Worker 名は `tax-pocket-test` 等、一目でテストと分かる名前にする。** URLは本人以外に渡さず、
  **下の宿題2件が済んだら消す**。同じ税額計算ツールが2つ公開状態で並ぶのが唯一の実害リスク
- **コード変更ゼロで並走できる。** `manifest.webmanifest` の `start_url`・`scope` は `"./"`（相対）なので、
  別URLに置いてもそのまま動く。**職員の端末は何も変わらない**（引っ越しではなく並走だから）
- **Cloudflare Access（URLに認証をかける機能）は今回かけない。** 本人のメールアドレスが Cloudflare 側に
  載るため、先に管轄の審査が要る。テストを短く終わらせる前提なら前倒しする理由がない

**2026-08-20 にここまで済んだ**

- `wrangler.jsonc` と `.assetsignore` を置き、`npx wrangler deploy` で Worker `tax-pocket-test` を作成。
  **75ファイルが上がり、本番と同じ画面が出た**（メニュー・「更新の確認」で v18 と数値表18件の日付を確認）
- ★**Workers は `.git` や `node_modules` を自動で除外しない**（Pages は除外していた。公式ドキュメントで確認）。
  `.assetsignore` を置かないと `.git` の中身まで配信対象になる（実測：除外前の読み取り996ファイル → 除外後75ファイル）
- **テスト用URLはこのファイルに書かない。** このリポジトリは公開されるため、書けば「本人以外に渡さない」が崩れる
- 認証は wrangler の OAuth。**要求される権限は28件と広く、この画面では絞れない**（Workers 以外に
  `pages`・`d1`・`email_sending` 等を含む）。取り消しは Cloudflare の My Profile →
  Access Management → Connected Applications、および `wrangler logout`

**★事故と対処（2026-08-20・同日中に解消）**

wrangler は自分の作業フォルダ `.wrangler/` を**配信対象のフォルダの中に作る**。
その中の `wrangler-account.json` には**アカウントIDとアカウント名（メールアドレス由来）**が入る。

- **配信されていた**（テストURLで HTTP 200）。原因＝`.assetsignore` に `.wrangler` を書いていなかった
- **コミットにも入れてしまった**。原因＝`git add -A`。**このリポジトリは公開される**
- **対処（すべて同日中）**：`.assetsignore` と `.gitignore` に `.wrangler` を追加／
  `git rm --cached` で amend（**push 前だったので公開はされていない**）／
  **Worker を削除して作り直した**（版ごとのプレビューURLに旧版が残っていて、そこからは読めたため）／
  `wrangler.jsonc` に `"preview_urls": false` を入れた
- **検証**：旧版のプレビューURL・新版のプレビューURL・本体URLのいずれでも 404。
  本体の `/`・`/sw.js` は 200 のまま（＝アプリは動いている）
- ★**教訓は2つ。**①**道具は自分の作業ファイルを作業対象のフォルダに置く**。配信・公開の対象を
  決めるときは「いま在るファイル」ではなく「これから増えるファイル」で考える。
  ②**公開リポジトリで `git add -A` を使わない。** 明示的にファイル名を並べる

**★配信ヘッダを本番に揃えた（2026-08-20）**

**放っておくと宿題1は測れなかった。** Cloudflare の既定は `Cache-Control: public, max-age=0, must-revalidate`
で、GitHub Pages は**全パス一律 `max-age=600`**（どちらも実測）。宿題1はもともと
「`cache: "reload"` で `max-age=600` を迂回できているか」という問いなので、
**600秒の壁が無い環境で通っても、本番で通る証明にならない**。

→ **`_headers` を1枚置いて本番と同じ壁を立てた**（`/*` に `Cache-Control: max-age=600`）。
`/`・`/sw.js`・`/src/app.js`・`/data/*.json`・`/manifest.webmanifest` の**全パスで一致を確認**。
`_headers` 自体は配信されない（404 で確認）。**GitHub Pages はこのファイルを解釈しないので本番は無影響**
（Jekyll が「_」で始まる名前を出力しないため）。仕様の出典（一次）＝
https://developers.cloudflare.com/workers/static-assets/migration-guides/migrate-from-pages/

★**この落とし穴は覚えておく価値がある。** 検証環境は「同じものが動く」だけでは足りない。
**測ろうとしている当のものが、環境の既定で消えていることがある。**

**★テスト中は `index.html` と `manifest.webmanifest` を手元で書き換えている（コミットしない）**

ホーム画面で本物と見分けが付かないと取り違えるため、**テスト側だけ表示名を「税額ポケット（テスト）」**にした。
書き換えているのは4か所＝`<title>`／`apple-mobile-web-app-title`（**iPhone のホーム画面の名前はこれ**）／
`.appbar__title`／`manifest` の `name`・`short_name`。

- **この2ファイルの変更はコミットしない。** Cloudflare へのデプロイは作業ツリーから直接上げるので、
  コミットしなくてもテスト側には反映される
- **テストが終わったら `git checkout -- index.html manifest.webmanifest` で戻す**
- `git status --short` にこの2つ以外が出たら、それは意図しない変更

**★宿題2件の結果（2026-08-20 実測・判断ログ D-36）**

1. **数値表の即日反映＝迂回できている（合格）。** 端末に `max-age=600` の壁を立て直した直後に
   配信側の日付を差し替え、アプリを閉じずに「更新の確認」を開き直したら**その場で入れ替わった**。
   → **改正した数値表は、職員が次にその画面を開いた時点で届く。10分待たない**
2. **Service Worker 更新の到達＝届く。ただし開き直しは2回要る。**
   v18 の端末に v19 を配信し、**1回目の開き直しでは v18 のまま、2回目で v19**。
   → **`docs/配布手順書` の「閉じて開き直す」を全て「1回で変わらなければもう一度」に直した**
   （`.md` と `.html` の両方）。**PDF の起こし直しは配布時まで保留**（2026-08-20 本人判断）。
   配るときに `node tools/build_tejunsho_pdf.mjs` を回すこと

判定はアプリ内の「更新の確認」で行った（測り方を新設していない。判断ログ D-25）。

**撤収した（2026-08-20・同日中）**

- Worker `tax-pocket-test` を削除／`wrangler.jsonc`・`.assetsignore`・`_headers` をリポジトリから削除／
  `index.html`・`manifest.webmanifest` の表示名を `git checkout --` で戻した
- **`.gitignore` の `.wrangler/` の行だけは残してある**（同じ事故を防ぐため）
- **iPhone のホーム画面の「税額ポケット（テスト）」は削除済み**（2026-08-20 本人）
- **Cloudflare の連携は解除済み**（2026-08-20・`wrangler logout`）。鍵のファイルが消えたことと
  `wrangler whoami` が未認証を返すことを実物で確認した。**再開するには `npx wrangler login` から。**
  そのときは `--use-keyring` を付けると、鍵が平文ファイルでなく Windows の資格情報マネージャーに入る
- **建て直したくなったら判断ログ D-36 を読む。** 3ファイル（`wrangler.jsonc`・`.assetsignore`・`_headers`）の
  中身と、`_headers` が要る理由（本番の `max-age=600` を再現しないと宿題1が測れない）を書いてある

**★経営ノート側に新テーマが立った（2026-08-20 本人指示）**＝「Cloudflare を今後どう使うか検証する」。
材料は `../keiei-notes/memory/内製ツール配布基盤-Cloudflare適用可否と税額ポケット判定.md` の
「★次の検証テーマ」節に集約済み。**tax-pocket 側でやることは今のところ無い**（引っ越しの是非が
その検証で決まったら、ここに戻ってくる）。

**未決のまま残っていること**（いずれも今日は決めていない）

- `main` へ push した瞬間に本番が変わる現状をどう変えるか（デプロイ経路の設計）
- 独自ドメインを噛ませるか。噛ませるなら**引っ越しを考えるより前**。2026-08-20 時点では決めない
- ★**引っ越し（GitHub Pages を止める）は別の話で、危険が違う。** 止めても端末は壊れず、
  cache-first なので**古い税率のまま静かに動き続ける**。税額計算のツールとして最も避けたい壊れ方

### 次のセッションの入口

**1. 2026-09-07 の未コミットの変更を片づける**（「いまここ」を読むこと）。
コミットして公開するかは本人の判断。公開するなら版は v20。

**2. 職員への聞き取りを1回で済ませる。**「①関与先で実際に開いたか ②次にどのメニューが欲しいか」に
**③いまの並びで使いにくいものはあるか**を足す。①②が成功条件の回収で、③は D-33 で決めた並びの
検算（並びは推測を含む。外れていたら `MENU` 配列の並べ替えだけで直る）。

**D-30 で見送った3点は、聞き取り後の版に束ねる**
（ふるさと納税の結果ラベルに「目安」／`--muted` のコントラスト／`.warn` の重み）。

第1版スコープは終わったので、次に何を作るかは `docs/仕様.md` の3基準で判定してから決める
（第2陣候補・検討中の候補は下記）。

職員から「メニューにふるさと納税が出ない」等の申告が返ってきたら、それを先に片づける。

**新しい計算メニューに着手する場合は**（相続税は v16 で実装済み）、`docs/仕様.md` の3基準を当て、
Plan モードで方針を出し、
**別コンテキストの敵対的レビューにかけてから実装する**（D-22・D-24・D-26・D-27・D-28・**D-29** で、
これが実際に致命的な穴を出している。D-27 は計画を1回まるごと破棄し、D-28 は土台の数値の誤りを、
D-29 は系統的に過大な数字が出る経路を見つけた）。

### 次に触る人へ（このプロジェクトで効いている作法）

- **条文は記憶で書かない。** 取る前に必ずリビジョン一覧
  `https://laws.e-gov.go.jp/api/2/law_revisions/<law_id>` を見て、**成立済み・未施行の改正**を確かめる
  （D-28 はこれを外して土台の数値を誤った）。年分で引く税目は「その年の12月31日までに施行される最後の版」
- **テストは実装より先に、条文から期待値を導いて書く。** D-27 はこれでデータの誤り（93万1円）を、
  D-29 は手計算した2事例で計算全体の正しさを検出・確認した
- **計画は別コンテキストの敵対的レビューにかける。** 省かない
- **「〜が検査している」とコメントに書いたら、その場で実在を確かめる**（D-28。存在しない検証ツールに
  守られているつもりになっていた）
- **モデルの配分**：条文・データ・テスト・計算ロジックは上位モデル、**画面のような大量定型は
  Sonnet のサブエージェントに委任**してよい（D-29 で実施。利用枠の温存になる）

### 検討中の候補（未判定・仕様.mdの3基準をまだ当てていない）

ユーザーから2026-08-06に提案があった。まだ「①関与先で判断が変わるか②毎年保守が発生するか
③既存ソフトで足りているか」の判定をしていないため、着手前に `docs/仕様.md` 方式での判定が要る。

- **所轄税務署・県税事務所の検索機能**（住所等から管轄の行政窓口を引く）
- **社会保険労務士業務向けの同種の行政窓口検索**（労働基準監督署・年金事務所等）
  → 税理士法人の職員向けツールである tax-pocket にそのまま載せるか、対象読者が違う別ツールにするかも
    未決定。次に触るセッションで project-kickoff の4軸診断を通してから決めること

**ユーザー判断が残っている1件（旧課題、未着手）** — セキュリティ診断の低-1（初回コミットの作者メール）

**着手前に必ず読む**：`CLAUDE.md`（設計原則7つ）／`docs/判断ログ.md`（D-10〜**D-29**）。

## 着手前に決める・確かめる

- [x] **GitHub リポジトリを作る** → **`sakurataxsys/tax-pocket`（public）**。private からの Pages 配信は Pro 以上が必要で、隠せるのはコードと履歴だけ＝見合わないと判断（判断ログ D-10）。個人アカウントで作成後、事務所の組織アカウントへ移管した（判断ログ D-13）
- [x] GitHub Pages を有効化し、push → 公開の経路を空のページ1枚で通した → **`https://sakurataxsys.github.io/tax-pocket/`** が HTTP 200（判断ログ D-11・D-13）。ブランチは `master` → `main` に改名済み
- [x] 職員の**人数感** → **16人以上**。手順書に「改正を反映したことの知らせ方」も含める（判断ログ D-12）

## 第1ステップ（単独完結・保守ほぼゼロの5メニュー）

**目的はメニューを揃えることではなく、配布方法と画面の型を確定させること。**

- [x] PWA の骨組み（manifest・service worker・オフラインキャッシュ）→ 判断ログ D-16
      - シェル＝版付き cache-first ／ `data/*.json`＝network-first（2秒で打ち切り）
      - **ロジック（`src/`）を変えたら `sw.js` の `CACHE_VERSION` を上げる。`data/` の差し替えだけなら不要**
- [x] 画面の型を1つ決める（内部ツール4原則／事務所ブランド配色は使わない）→ 判断ログ D-15
      - 単一HTML＋ハッシュルーティング。計算画面の型＝適用年分／入力/結果カード/計算過程/根拠・対象外
- [x] 退職金（退職所得）→ 判断ログ D-17
- [x] 減価償却費（定額法・定率法）→ 判断ログ D-18
      - 中古資産の簡便法／取得価額による少額の注意表示（10万・20万・40万）も入れた
      - **償却率表は省令の別表から自動生成する**（`node tools/fetch_shokyakuritsu.mjs`）。手で書き写さない
- [x] 印紙税 → 判断ログ D-20
      - 別表第一の全20号（第17号は1・2に分割して21択）＋措置法91条の軽減。作成年月日で本則／軽減を切り替える
      - **税額表は別表第一と措置法91条から自動生成する**（`node tools/fetch_inshizei.mjs`）。手で書き写さない
      - 軽減表は本則より1区分少ない（下限以下は本則へ落とす）。号の判定（通則1〜5）は行わない
- [x] **登録免許税** → 判断ログ D-22
      - 範囲は別表第一の**第1号（不動産の登記・葉40件）と第24号（会社の商業登記・葉29件）のみ**
      - **税額表は別表第一と措置法72条・72条の2・73条・75条から自動生成する**（`node tools/fetch_toroku_menkyozei.mjs`）。手で書き写さない
      - 収録開始日＝**令和4年9月1日**（支店所在地登記の廃止）。e-Gov の時点指定で二分探索して確定した
      - 軽減は行に直付けせず、対象（土地／売買／住宅用家屋）を文言に書いた**相互排他のセレクト**にした
      - 適用期間の判定日は条文で違う（72条＝登記日／72条の2・73・75＝新築・取得の日）
      - 第24号（一）ホ・ヘ（二段税率）と登免法17条は扱わない。選んだら理由を出す
- [x] 延滞税・利子税 → 判断ログ D-21
      - 年別の割合は式で導かず、数値をそのまま持つ（令和2年度改正で利子税の式が変わっているため）
      - 「納期限」と「法定納期限」を別入力にし、期限後申告・修正申告に対応
      - 通則法61条の期間の特例は控除しないが、該当しうる入力では警告を出す
- [x] 退職金のテスト（端数処理・境界値・改正前後の分岐）17件 — `npm test`
- [x] 減価償却のテスト（同3種＋償却率表の取り込みミス検知）22件 — `npm test`
- [x] 印紙税のテスト（区分の境界・非課税の境界・軽減の適用期間＋税額表の取り込みミス検知）29件 — `npm test`
- [x] 延滞税・利子税のテスト（端数処理・2月を経過する日の境界・年またぎ＋割合表の検算）23件 — `npm test`
- [x] 登録免許税のテスト（端数処理・境界値・改正前後の分岐＋税額表の取り込みミス検知）30件 — `npm test`

## 参照2画面（第1ステップと並行可）

- [x] 税額表・社会保険料率等のリンク集 → 判断ログ D-23
      - URL は `data/link_shu.json`。13件を4分類（源泉徴収・給与／社会保険・労働保険／資産税・評価／そのほか）
      - 各リンクに**適用年度**を名称の右に表示し、リンク集の**最終更新日**を画面下に出す
      - 全URLを実際に取得して200を確認した（記憶で書かない）
- [x] 法人税の実効税率・均等割 早見表 → 判断ログ D-24
      - 率はすべて条文で確定（法人税法66条／措法42条の3の2／地方法人税法10条／地方税法51・52・312・314の4・72の24の7／特別法人事業税法7条）
      - 対象は**資本金1億円以下の普通法人**・**すべて標準税率**。所得は入力させず7段階の固定行で組む
      - 収録開始日＝**令和7年4月1日**（17%が条文に入った日）。e-Gov の時点指定で確定した
      - 軽減が使えない法人3類型・外形標準課税・均等割の判定基準は、実装せず画面に明記した

## 第2ステップ

- [x] **所得税エンジン**（課税所得の計算。メニューには出さず内部に持つ）→ 判断ログ D-27
      - **所得税ベースと住民税ベースの2系統を同時に返す**。ふるさと納税の正式方式（地方税法37条の2
        第11項）は住民税ベースの課税総所得金額と人的控除差調整額を要し、所得税ベースの控除合計からは
        導出できないため。**人的控除は「控除額」ではなく「事実」で受け取る**
      - **別表第五は表を持たず**、しきい値と刻みから再現している
      - 分離課税5区分を金額入力で受ける。**合計所得金額に入るのは特別控除「前」**（措法35条1項1号が
        読み替えるのは課税長期譲渡所得金額の側だけ）
      - 配偶者特別控除・特定親族特別控除は**公表の控除額表と全区分一致**を確認
- [x] **所得税エンジンの令和8年分対応** → 判断ログ **D-28**
      - 令和8年度改正（令和8年法律第12号・第2号）で、**令和8年分から**給与所得控除が構造ごと変わり
        （360万円以下→8万円＋収入×30%、最低保障69万円）、基礎控除が62万円、措置法の上乗せが
        489万円以下42万円／超5万円、扶養親族等の所得要件が62万円になる
      - ★原因の記録：**e-Gov の現行施行版だけを見て、成立済み・未施行の改正を見落としていた。**
        条文を取る前に `law_revisions` で施行日が先の版を必ず見る（`docs/改正対応手順.md` に手順を追加）
      - ★**別表第五の刻みの開始が4,000円の倍数でない**（令和8年分は2,026,000から始まり最初の1区分だけ
        幅2,000円）。下限を区間の開始額で止めるよう `calc_kyuyo_shotoku` を直した
      - **`tools/verify_besshi5.mjs` を作った**（コメントには実在すると書いてあったが無かった）。
        原文の別表第五と全行照合する。令和7年分3,529点・令和8年分3,436点が一致
      - **収録は令和7年分・令和8年分の2年分**。令和9年分以後はひとり親控除が動くため未収録
      - テスト251件（`npm test`）
- [x] **ふるさと納税シミュレーター**（住民税所得割に乗る）→ 判断ログ **D-29**
      - 出力は**限度額だけ**。**計算方式は画面上部のセレクトで切り替える**（既定＝正式方式）。
        当初は両方式を並べる作りにしたが、実機を見たユーザー判断で切り替え式に変更した
      - 限度額＝A（特例控除の20%上限からの逆算）／B（寄附金額の30%）／C（所得税の40%）の**最も小さいもの**。
        **B・Cは非負の入力では数学的に発動しない**ことを確かめた（テストで作ろうとしないこと）
      - 割合表は `data/furusato.json` に**附則5条の6の読み替え後の固定値**で持つ（式にしない）
      - ★**37条の2第11項の2号と3号を計画で取り違えていた**（90%は2号）。原文で訂正済み
      - ★**正式方式と簡易方式の差は「物的控除の所得税と住民税の差」だけ**。生命保険料控除・
        地震保険料控除・ほかの寄附金控除が無ければ両方式は一致する（D-29 に導出を残した）
      - ★**扶養親族の入力に「16歳未満」の欄が要る**（所得金額調整控除。敵対的レビューの致命的1件）
      - ★エンジンの `soshotoku_kingaku_to` を削除した（30%上限の分母として誤り）
      - ★令和9年分の寄附から入る**特例控除額の総額上限**は、版は作らないが**読む処理は実装済み**
      - **セキュリティ診断を1回回した**（重大0・中1・低3。中1・低3とも対応済み）
      - テスト251件→**303件**（`npm test`）

## 第3ステップ

- [x] **相続税の概算** → 判断ログ **D-31**
      - 出力は**相続税の総額＋納付総額の2パターン**（配偶者が法定相続分を取得した場合／取得しない場合）。
        取得割合は入力させない。前提は結果カードのラベルに書く
      - **税率表は相続税法16条の表から自動生成する**（`node tools/fetch_sozokuzei.mjs`）。手で書き写さない。
        速算表の控除額は法令に無いので持たず、区分から積み上げる
      - ★**着手前の敵対的レビュー（Fable）で致命的3件**。①保険金と退職手当金の非課税枠は
        12条1項6号・7号で**別枠**なのに1欄にしていた ②**代襲相続の入力が無く**、孫養子欄に入れると
        基礎控除が過少＋2割加算が誤適用 ③19条は**人ごとの規定**なのに贈与加算を合算で受けていた。
        すべて実装前に計画へ反映した
      - ★**e-Gov の要素指定（`?elm=`）は400になるが、全文XMLから条を切り出せば表まで取れる。**
        tax-law MCP の `get_law` は条文中の表を「（表省略）」で返すため、表を要する実装には使えない
      - 検算：配偶者＋子2人・課税価格2億円で総額**2700万円**／配偶者が法定相続分なら納付**1350万円**
      - テスト303件→**337件**（`npm test`）

## 配布

- [x] **iOS でのホーム画面追加手順を実機で確認した**（2026-08-05）
      - 手順書の文言は実機と一致。アイコン名も「税額ポケット」になる
      - **Edge からも追加できた** → 「Safari 以外は不可」と書いていたのは誤りで、訂正済み
      - 機内モードで「更新の確認」の日付は瞬時に出た（手元のブラウザでは約2秒かかっていた）
- [x] **iOS で全メニューを機内モードで開き、最後まで計算できることを確認した**（2026-08-05）
- [x] ホーム画面に追加した直後、アイコンから1度も開かずに機内モードで開いた → **計算まで通った**
      - ただし**この結果は決め手にならない**。アイコンを削除しても iOS がそのWebアプリの保存データまで
        消すとは限らず、**同じ端末で先に一度アイコンから開いて使っていた**ため、その古いデータが
        再利用された可能性が残る。一度も追加したことのない端末でなければ切り分けられない
      - **手順書は「追加したら、アイコンから開いて各メニューを1回ずつ表示する」を残す判断にした。**
        職員の手間は30秒で、不要でも害がない。逆に必要だった場合は関与先で開かない端末が出る。
        仮に抜けても、税率表の無い端末には画面に「通信できる場所で一度開くと…」と出るので黙って壊れはしない
      - 根拠：ホーム画面のWebアプリは**ブラウザとクッキー・ストレージが別**（Apple 公式・WWDC23
        「What's new in web apps」<https://developer.apple.com/videos/play/wwdc2023/10120/>）
- [ ] **Android での手順と機内モード動作を実機で確認する**（実機が手元にないため未実施）
      - 手順書の Android の項は「ホーム画面に追加」「アプリをインストール」の**両方の表記を併記**してあるため、
        表記が違っても職員は迷わない。**配布は Android の確認を待たずに始めてよい**
      - Android を持つ職員が最初に追加するときに、表記と機内モード動作を聞いて確定させる
- [x] 手順書を1枚作り、確定させた → `docs/配布手順書.md`（正本）／`docs/配布手順書.html`（配布・印刷用）
      - **★ 8/5 の訂正**：当初「WebKit の ITP 2.3 でホーム画面アプリは7日削除の対象外」と書いたが、
        **原文にその記述は無かった**（検索結果の要約を鵜呑みにしていた）。原文で確認できたのは
        「Safari は7日間使わないとサイトの保存データを削除する」までで、ホーム画面アプリの扱いは書かれていない。
        論拠は上の Apple 公式（ストレージが別）に置き換えた
      - **★2つのファイルは内容が同じ。片方だけ直さないこと**
- [x] **改正を反映したことを職員に知らせる経路を決めた** → 判断ログ D-25
      - **チャットワークの「税額ポケット」グループに投稿**＋アプリに**「更新の確認」画面**（`#/koushin`）を足した
      - **アプリ側には連絡手段の名前を書かない**（手段が変わるたびに版が上がり再配信が要るため）。
        名前が出るのは `docs/配布手順書.md`・`docs/配布手順書.html`・`docs/改正対応手順.md` の3つだけ
      - 投稿には**差し替えた JSON に書いた日付そのもの**を書く（投稿日ではない）。文例は `docs/改正対応手順.md`
      - 版は `sw.js` に問い合わせず `src/version.js` の `APP_VERSION` に持たせた
        （SW に聞くと、新しい版名のキャッシュに旧シェルが入っている事故のとき画面が嘘をつく）
- [x] 免責の文言を確定して画面に置く（全画面のフッターに常設。準備中ページの文言をそのまま引き継いだ）

## 検証・記録

- [x] **セキュリティ診断を1回回した**（2026-08-05・読み取り専用サブエージェント）→ 判断ログ「セキュリティ診断の記録」
      - 重大0・中0・低3。配布前に必ず直すべきものは無し
      - 低-2（meta CSP）と低-3（recalc の try/catch）は対応済み
- [ ] **低-1 の判断：初回コミットの作者メールが公開履歴に残っている**（Apple Private Relay のエイリアス）
      - 2コミット目以降は GitHub の noreply。履歴書き換えは配布済みURL・クローンに波及する破壊的操作のため推奨しない
      - GitHub の「Block command line pushes that expose my email」を有効化するか、Apple 側でエイリアスを無効化するかを決める
      - ★2026-08-06：**この項目自体がエイリアスを平文で書いていた**ので伏せた（このリポジトリは公開される。
        グローバル規約「個人情報をリポジトリ内の全文書に含めない」）。過去のコミットには残っているため、
        上の判断はこの文書の履歴にも及ぶ
- [ ] 配布後、職員への**聞き取り2問**（①関与先で実際に開いたか ②次にどのメニューが欲しいか）＝これが成功条件
      - ★**③メニューはどの順がよいか**を同じ場で聞く（メニュー順のA案／B案を分ける唯一の材料。D-30）

### 積み残した確認（2026-08-04 時点）

- [x] **本番でのシェル更新を観測した（2026-08-06・v10 → v14 の公開時）**
      - push から**約9分**（525秒）で配信が v10 → v14 に切り替わった
      - **切り替えは一斉だった。** `sw.js`・`src/app.js`・`src/version.js`・`src/calc/furusato.js`・
        `data/furusato.json` の ETag がすべて同じ接頭辞（`6a74555d-`）で、
        **配信側が一部だけ古いものを返す状態は観測されなかった**
      - 初回訪問で service worker が install し、`tax-pocket-shell-v14` に**新しい `app.js` が入った**
        （22件。キャッシュ内の `app.js` に今回追加した切り替え式のコードが含まれることを確認）
      - **8月5日の事象（新しい版名のキャッシュに旧シェルが入る）は再発しなかった。**
        候補①（配信側が旧 `app.js` を返している間に install する）ではなく、
        候補②（デプロイ完了前に開いていたページが旧 service worker のまま動いていた）だった可能性が高い
      - ★**2026-08-20、検証環境で作為的に測って決着した（判断ログ D-36）。**
        v18 を入れた iPhone に v19 を配信したところ、**1回目の開き直しでは v18 のまま、
        2回目の開き直しで v19** になった。版付き cache-first ＋ `skipWaiting()` の想定どおり
        （1回目の裏で取り込み、すでに描画済みの画面は古いシェルのまま）。
        **職員への案内は「閉じて開き直す。まだ古ければ、もう一度」**。配布手順書は直してある
      - 参考：2026-08-10 に v15 の iPhone が v16 になったのを観測したが、**開き直した回数を
        数えていなかった**。1回で足りるという当時の書き方は、上の実測で置き換えた
- [x] **税率表を差し替えて即日反映されることを確認した（2026-08-20・Cloudflare の検証環境で。判断ログ D-36）**
      - **本番と同じ `max-age=600` を `_headers` で立てた上で測り、その場で新しい日付に入れ替わった。**
        `cache: "reload"` は効いている。以下は決着前の記録として残す
      - `data/*.json` の取得に `cache: "reload"` を付けて GitHub Pages の `Cache-Control: max-age=600` を迂回している（判断ログ D-16）。**ヘッダが実際に付いていることは確認済み**だが、迂回できているかはページ側から測ろうとしても service worker が応答を返すため区別できない
      - 実データを1回デプロイしないと確かめられないため、**最初の改正対応のとき（`docs/改正対応手順.md` を埋める作業）にあわせて確認する**
      - ローカルでの等価な確認（税率表を書き換え→メニューに戻って入り直すだけで反映）は通っている
      - **★ 2026-08-05 に v9 → v10 のシェル更新を公開した。**まだ誰も開いていないため、
        配布時が本番でのシェル更新の初めての観測機会になる。職員から
        「メニューに『更新の確認』が出ない」と申告が出たら、下の①が再発したということ
      - **2026-08-05 の観測**：早見表を公開した直後、本番を開いていたブラウザが
        `tax-pocket-shell-v8`（新しい版名）のキャッシュを持ちながら、**中身は旧版の `app.js`** で、
        メニューに「法人税の早見表 準備中」が出続けた。キャッシュを消して読み直すと直った。
        原因は特定できていない。候補は2つ ——
        ①GitHub Pages の配信側がまだ旧 `app.js` を返している間に service worker が install し、
        新しい版名で旧内容を抱えた（`cache: "reload"` はブラウザのキャッシュを迂回するが配信側は迂回しない。判断ログ D-16 が想定している事故）
        ②デプロイ完了前に開いていたページが、旧 service worker のまま動いていただけ
      - ★**この確認をするときは、シェル（`src/`）の更新とデータ（`data/`）の更新を分けて観測する。**
        職員に影響するのは①で、起きているなら「改正を反映したことを職員に知らせる経路」の設計が変わる
      - ★**2026-08-20、これを Cloudflare の検証環境で測ると決めた**（上の「Cloudflare へのアップテスト」）。
        **「実データを1回デプロイしないと確かめられない＝最初の改正対応まで待つ」という前提はここで崩れる。**
        本番（GitHub Pages）に触らず、複製の側で税率表を差し替えて測る
- [ ] **暦年贈与の加算期間の経過措置を、改正法附則の原文で確認する**（判断ログ D-31）
      - 現状は**国税庁タックスアンサー No.4161（二次情報）の表**で押さえている。
        令和5年法律第3号の附則の原文は**未確認**（e-Gov の v2 `law_data` は改正法IDで404、
        `elm=SupplProvision` は昭和25年当時の附則しか返さない）
      - 押さえている内容：相続開始日が〜令和8年12月31日は3年以内／令和9年1月1日〜令和12年12月31日は
        令和6年1月1日から死亡の日まで／令和13年1月1日〜は7年以内。
        100万円控除は**相続開始日が令和9年1月2日以後**
      - ★**この確認が効いてくるのは令和9年（2027年）1月から。**それまでは加算対象が3年以内だけなので、
        表が誤っていても出る数字は変わらない。**令和8年中に確認しておく**
      - 確認できたら `data/sozokuzei.json` の「贈与加算」の「根拠の確認状況」を書き換える

- [ ] **短期退職手当等の適用開始時期を、改正法附則の原文で確認する**
      - 現状は「令和4年1月1日以後に支払を受けるべき退職手当等から」を国税庁パンフレット（二次情報）で押さえている状態。`data/taishokukin.json` の版の区切りがこれに依存している
      - ★手順の目処が立った：e-Gov 法令API v2 で改正法の附則を原文で読める（減価償却で実施済み。判断ログ D-18）

- [ ] **減価償却の償却限度額の円未満端数処理に、明文の根拠があるか確認する**（判断ログ D-18）
      - 法令63条の2は財務省令へ委任するのみで、委任先に端数の定めを確認できていない。現状は国税庁の設例に従い切捨て

- [ ] **令和3年分以後の利子税の割合を、国税庁の公表値そのもので確認する**（判断ログ D-21）
      - 現状は国税庁公表の延滞税の割合から措置法93条2項・94条1項の式で算出した値（令和3年1.0%／令和4〜7年0.9%／令和8年1.3%）
      - `data/entaizei.json` の各年に出典区分「条文から算出」と明記してある。公表値を確認できたら「公表」に直す
      - 令和2年分までは公表値をそのまま採っている（区分「公表」）

- [ ] **外形標準課税の「減資対応」の類型を、改正法附則の原文で確認する**（判断ログ D-24）
      - 早見表の「扱わないもの」には、地方税法72条の2第1項1号ロ（**本則で確認済み**）の
        「払込資本50億円超の特定法人の100%子法人で払込資本2億円超」だけを書いてある
      - 令和6年度改正にはもう1つ、減資への対応として「資本金＋資本剰余金の合計が10億円超で
        前事業年度に外形対象だった法人」の類型があるとされるが、**附則の原文を未確認**のため書いていない
      - 確認できたら `data/hojinzei_hayami.json` の「扱わないもの」に追記する

- [ ] **第17号の非課税枠が3万円未満→5万円未満になった適用時期を、改正法附則の原文で確認する**（判断ログ D-20）
      - 現状は国税庁の一般周知（二次情報）で「平成26年4月1日以後に作成される受取書から」と押さえている状態
      - `data/inshizei.json` の `収録開始日`（2014-04-01）はこれと軽減税率表の改正の両方に依拠している。
        軽減税率表だけでも収録開始日は成り立つため結論は変わらないが、「本則は変わっていないから
        平成26年より前も開放してよいのでは」という将来の誤判断を防ぐために確認しておく

## 保守の足回り

- [ ] **GitHub Actions で `npm test` を回す（CI が無い）**
      - 属人化対策の想定運用は「税務職員が `data/` を1ファイル差し替える」（判断ログ D-07・D-13）。
        職員は手元でテストを走らせない。**いまの検算はすべて開発者のマシンでしか動いていない**
      - 現に `data/*.json` には、テストが守っている前提が複数ある
        （延滞税の2つの割合の差は6.3ポイント／税額表の取り込みミス検知／日付キーの決め打ち）
      - 職員へ書き込み権限を渡すのは「改正対応を1回回して手順書が埋まってから」（D-13）。
        **権限を渡す前に CI を入れる**（渡した後では、最初の1回が守られない）

## 保守（毎年）

- [ ] 改正対応を1回実際に回してから、その手順を `.claude/skills/` にスキル化する（先回りして書かない）
- [ ] `docs/改正対応手順.md` を1回目の実作業で埋める

## 第2陣候補（第1版では作らない）

個人VS法人比較／贈与税／譲渡税／不動産取得税
→ 判定の根拠は `docs/仕様.md` の判定表を参照

## メニューの並び（決着した。判断ログ D-33）

**端末ごとの並べ替え（A案）は作らない。`data/menu.json` で配る仕組み（B案）も作らない。**
`src/app.js` の `MENU` 配列の並びを事務所が決める。2026-08-10 に次の順にした。

和暦・西暦／減価償却費／退職金／ふるさと納税／相続税／延滞税・利子税／
印紙税／登録免許税／法人税の早見表／リンク集／更新の確認

- **並びは頻度ではなく「画面の性質」で決めた。** 先頭が和暦・西暦（税額計算ではないが
  書類の日付で場面を選ばず使う）、次が計算を伴うもの、後ろが**表を引くだけのもの**
- ★**印紙税と登録免許税は「参照」の塊に置いた。** 税額計算メニューだが、職員の操作は
  「別表の該当する行を探す」で、法人税の早見表やリンク集と変わらない。
  **メニュー上の分類は税目ではなく操作で決める**
- A案を採らないのは、**このアプリがいっさい永続保存を持たない**という、
  3回のセキュリティ診断が確認事項として記録している前提を崩さないため
- B案を作らないのは、**並びをリリースなしで変えたい場面がまだ一度も起きていない**ため
  （単純さ最優先・Rule of Three）。必要になったらそのとき移る
- **更新の確認は必ず最後**に置く。どこにあるかを配布手順書に書いてあるため
- 聞き取りで「いまの並びで使いにくいものはあるか」をあわせて聞き、外れていたら
  配列の並べ替えだけで直る

---

## 検討中の候補（未判定・2026-08-06 提案）

- 所轄税務署・県税事務所の検索機能
- 社会保険労務士業務向けの行政窓口検索（労働基準監督署・年金事務所等）

3基準（①関与先で判断が変わるか②毎年保守が発生するか③既存ソフトで足りているか）を未適用。
tax-pocket に載せるか別ツールにするかも未決定（詳細は「いまここ」参照）。
