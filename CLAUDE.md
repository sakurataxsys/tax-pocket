# CLAUDE.md — tax-pocket（税額ポケット）

> ## ★このリポジトリは GitHub 上で公開される
>
> GitHub Pages で配信するため、**コミットした内容は誰でも読める**。
> 事務所の他プロジェクト（`system-ledger` 等・ローカルgitのみ）とは前提が違う。
> **顧問先名・個人名・実際の金額・APIキー・社内URL・IPアドレスを、コード・コミットメッセージ・ファイル名・テストデータのいずれにも書かない。**
> テストデータは必ず架空の値を使う。

客先を回る職員が iPhone / Android どちらでも使える税額計算ツール。PWA として GitHub Pages で配信する。
アプリ表示名は **「税額ポケット」**（ホーム画面のアイコン名）。

- 経緯・意思決定の正本 → `../keiei-notes/memory/職員向け税額計算ツール-第1版スコープと配り方.md`
- 第1版スコープと22項目の判定 → `docs/仕様.md`
- 設計判断の記録 → `docs/判断ログ.md`

---

## 設計原則（外すと後から直せない）

1. **完全クライアントサイド** — 入力した金額をサーバーへ送らない。顧問先の数字が外部に出る経路を構造的に作らない
2. **入力値をURLに乗せない** — 結果共有機能を作る場合の禁止事項。URLは履歴・アクセスログに残る
3. **数値表もリンクURLもコードに埋めない** — すべて `data/` の外部データファイルに置く。改正時の作業を「1ファイル差し替え」に落とし、税務がわかる職員なら誰でもできる状態にする（属人化対策）
4. **オフラインで動く** — ロジックと数値表は端末にキャッシュし、オンライン時に裏で最新を取りに行く。客先で電波が悪くても直近版で動く
5. **事務所名・ロゴを入れない ＋ 免責を1行置く** — 事務所名を出すと「事務所が公開している税額計算ツール」になり、一般の人が使って誤った税額を得たときの対外的な責任が生じる
6. **リンクには適用年度を必ず表示する**（例「令和8年分 源泉徴収税額表」）＋ リンク集の最終更新日を画面に出す
7. **数値そのものをアプリに持つのは Rule of Three 後** — リンクで運用して「毎回開くページ」が3つ以上見えてから、その数個だけ取り込む

### 原則6の理由（忘れやすいので明記）

危険なのは**リンク切れではなく、古いページが正常に開くこと**。
国税庁の源泉徴収税額表や協会けんぽの保険料額表は**年度ごとに別ページとして新設**されるため、去年のURLを貼ったままだとリンクは生きていて、正しく表示され、中身だけが古い。誰も気づかない。
年度が画面に見えていれば人が気づける。これが更新漏れに対する最後の防波堤。

---

## 税務ロジックの規律

- **条文・通達は記憶で書かない。** 税率・控除額・基準額を実装するときは tax-law MCP 等で原文を取得し、根拠を `docs/判断ログ.md` に残す
- **端数処理は法令準拠**とし、切捨て／切上げ／四捨五入の別をコードのコメントに明示する
- **テストケース必須**。最低限、①端数処理 ②境界値 ③改正前後の分岐 の3種を書く
- **改正は上書きせず版で持つ**。過年度の試算（修正申告・遺産分割のやり直し）を客先で出すため、旧ルールは消さない
- 不確かな解釈は「要確認」とコメントに残し、確定情報として画面に出さない

---

## 第1版スコープ（詳細は docs/仕様.md）

**計算7メニュー**：退職金／減価償却費／印紙税／登録免許税／延滞税・利子税／相続税の概算／**ふるさと納税シミュレーター**
**参照2画面**：法人税の実効税率・均等割 早見表／税額表・社会保険料率等のリンク集

**毎年ロジックに手を入れるのは「所得税エンジン」1箇所だけ**。この集約が第1版設計の要で、機能追加のたびにこの前提が崩れていないか確認する。

### やらないこと（スコープ外を明示する）

- 申告書の作成・提出の代替にしない
- **給与計算・賞与・手取から給与逆算を作らない**（源泉徴収税額表＋社会保険料率で年に複数回更新＝保守の重心）
- **法人税の計算機を作らない**（所得×約30%で会話は成立する。掛け算で出ない「軽減税率の区切り」と「赤字でも出る均等割」だけ早見表で担保）
- 宅地の評価（路線価方式）を作らない（路線価データの毎年調達が最も重い）
- 年末調整・消費税・固定資産税を作らない
- 顧問先データを保持しない。ログイン機構を作らない（客先での速さが死ぬため、認証を置けないことが機能要件）
- 一般向けサービスとして運営しない（公開はするが対象は職員）

---

## 作る順序

1. **第1ステップ**：退職金 → 減価償却 → 印紙税 → 登録免許税 → 延滞税・利子税
   計算エンジンを共有しないので相互に波及しない。**ここで配布方法と画面の型を確定させる**
2. **第2ステップ**：所得税エンジン → ふるさと納税（限度額は住民税所得割に乗るため、課税所得の計算がまるごと必要）
3. **第3ステップ**：相続税の概算

参照2画面は第1ステップと並行で置ける。

---

## 見た目

内部ツールの4原則（グローバル CLAUDE.md）に従う。**事務所ブランド配色（紺・金・桜）は持ち込まない**（原則5と同じ理由で、公開物では特に守る）。
ダークモードは実装しない。

---

## 成功条件

利用実績は**技術的に取得できない**（完全クライアントサイド・認証なしの設計上のトレードオフ）。
代わりに第1ステップ配布後、職員への聞き取り2問で判定する：

1. 客先で実際に開いたか
2. 次にどのメニューが欲しいか

---

## セキュリティ診断

グローバル規約の実施契機のうち **①職員に配る ③外部と通信する経路** の2つに該当する。
実装の前後どちらかで読み取り専用のサブエージェント診断を1回回し、結果を `docs/判断ログ.md` に残す。
