{
  "name": "tax-pocket",
  "version": "0.1.0",
  "private": true,
  "description": "税額ポケット — 客先で使う税額計算ツール（PWA）",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "node tools/serve.mjs"
  }
}
